import Head from "next/head";

const IsHead = () => {
  return (
    <Head>
      <title>Send Me Anonymous Message</title>
      <meta name="description" content="send me anonymous messages now" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default IsHead;
